import { WrapItem } from '@chakra-ui/react';

const TodoListItem = () => {
  const properties = {
    imageUrl: '',
    imageAlt: '',
    title: 'RO: To Do List',
  };

  const { title } = properties;

  return (
    <WrapItem>
      <div>111</div>
    </WrapItem>
  );
};

export default TodoListItem;
